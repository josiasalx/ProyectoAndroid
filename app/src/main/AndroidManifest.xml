<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.proyecto">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".SplashScreen"></activity>

        <activity android:name=".Daytime1" />
        <activity android:name=".GCena" />
        <activity android:name=".GAlmuerzo" />
        <activity android:name=".GDesayuno" />
        <activity android:name=".Cena" />
        <activity android:name=".Almuerzo" />
        <activity android:name=".Desayuno" />
        <activity android:name=".Daytime" />
        <activity android:name=".Option" />
        <activity android:name=".SignUp" />
        <activity android:name=".Login" />
        <activity android:name=".MainActivity"
            android:theme="@style/SplashTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>